<h2 class="ui center aligned icon header">
    <i class="lock icon"></i>
    <div class="content">
        {{ t._('er_Unauthorized') }}
        <div class="sub header">{{ t._('er_UnauthorizedDescription') }}</div>
    </div>
</h2>
