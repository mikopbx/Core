server {
    listen      <WEBPort>;
    server_name   default;
    resolver <DNS>;

    more_clear_headers Server;
    include mikopbx/security-headers.conf;

    # locations files
    include mikopbx/locations/*.conf;

 	# module locations files
    include mikopbx/modules_locations/*.conf;
}